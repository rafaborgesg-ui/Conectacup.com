// Script para corrigir a linha 544
// Substituir: firstEmptyIndex por firstEmptyOriginalIndex

// Linha 544 atual:
// const firstInput = document.querySelector(`input[data-jogo="${set.jogo}"][data-position="${firstEmptyIndex}"]`) as HTMLInputElement;

// Linha 544 corrigida:
// const firstInput = document.querySelector(`input[data-jogo="${set.jogo}"][data-position="${firstEmptyOriginalIndex}"]`) as HTMLInputElement;
